debug: true

server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  mvc:
    servlet:
      load-on-startup: 1
  data:
    rest:
      base-path: /api